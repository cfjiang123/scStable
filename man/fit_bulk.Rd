% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bulk.R
\name{fit_bulk}
\alias{fit_bulk}
\title{Fit the bulk RNA-seq distribution for scStable}
\usage{
fit_bulk(
  bulkRNA_matrix,
  n_cores = 20,
  p_val_threshold = 0.5,
  step = 1,
  c_value_max = 200,
  normal_test = c("auto"),
  sw_limit = 200
)
}
\arguments{
\item{bulkRNA_matrix}{Numeric matrix of bulk RNA-seq counts (genes x samples),
typically the \code{bulk} element returned by \code{\link{synthreplicate_prep}}.}

\item{n_cores}{Integer; number of worker cores for the parallel grid search.}

\item{p_val_threshold}{Numeric; normality-test p-value at which the search for
\code{c} stops early.}

\item{step}{Numeric; increment of the \code{c} grid.}

\item{c_value_max}{Numeric; maximum value of \code{c} to evaluate.}

\item{normal_test}{Character; \code{"auto"} chooses Shapiro-Wilk for small
samples and Anderson-Darling otherwise; may also be \code{"shapiro"} or
\code{"ad"}.}

\item{sw_limit}{Integer; sample-size ceiling under which Shapiro-Wilk is used
when \code{normal_test = "auto"}.}
}
\value{
A list with elements \code{bulk_data_counts} (log-transformed counts),
\code{mu} (gene means), \code{cov} (gene-gene covariance) and
\code{optimal_c} (per-gene log offsets).
}
\description{
\code{fit_bulk} estimates, for each gene, a log-offset constant \code{c} that
best normalises the bulk RNA-seq counts (\code{log(count + c)}), then computes
the gene-wise mean vector and the gene-gene covariance matrix used by
\pkg{scStable} to sample synthetic bulk replicates.
}
